[build]
builder = "nixpacks"
buildCommand = "pip install -r edgar-api/requirements.txt"

[deploy]
startCommand = "cd edgar-api && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
